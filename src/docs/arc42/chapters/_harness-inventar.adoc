// Erzeugt von scripts/harness-rad.js – nicht von Hand ändern.
[cols="2,2,5,4",options="header"]
|===
| Abschnitt | Stufe (Tier 2) | Nachweis | Lücke
| Build & Language | 3 von 5 (60 %) | Compiler / Parser: `npm test` und `npm run build` laden jedes Modul; Syntaxfehler brechen `test-und-build`; Type checker: `tsc --checkJs` mit `strict` im Pflicht-Check, vorerst nur `src/kern` (`jsconfig.json`), #25; Linter: ESLint (`eslint.config.js`: no-eval, no-implied-eval, no-unsanitized) im Pflicht-Check, #21 | Formatter (offen); Import sorter / dead code (offen)
| Testing | 3 von 3 (100 %) | Unit tests: rund 500 `node:test`-Tests in `test/` und `src/<app>/test/`; Property-based / fuzz: fast-check, 17 Properties, #22: Zahlen und Terme (`test/kern/*.property.test.js`, Fund #30 behoben); jede Rechnung im Lösungsweg jeder App wird nachgerechnet (`test/apps/rechenweg.property.test.js`, L-021); Integration tests: Vertragstest `test/apps/vertrag.test.js`: jede Kompetenz jeder App, Seeds 1–20; Contract tests: Tutor-Vertrag `lib/llms-vertrag.js`: Deep Links in `llms.txt`/`tutor.md` gegen Generator-Parameter; End-to-end / UI: Playwright (Chromium) in CI, Workflow `browser.yml`, #26: Übungsrunde je App, „Zurück zu Claude“, 360 px, ohne JS (`e2e/`); Smoke tests: `e2e/smoke.spec.js`: jede Seite HTTP 200, 0 Konsolenfehler; `e2e/extern.spec.js`: 0 fremde Requests, #26 | –
| Security | 3 von 5 (60 %) | Secret scanning: GitHub Secret Scanning mit Push Protection; SCA: Dependabot-Alerts und -Security-Updates; `npm audit --audit-level=high` im Pflicht-Check, #20; SAST: CodeQL Default Setup (JavaScript/TypeScript, Actions); Threat modeling: STRIDE in arc42 8.1 | License compliance (offen); LLM security review (geplant: #24 KI-Code-Review vor jedem Merge)
| Architecture | 1 von 4 (25 %) | Code review: Product Owner prüft und merged jeden PR; kein Pflicht-Approval; ADR enforcement: `lib/pruefungen.js` bricht den Build bei Regelverstößen (ADR-008), auch bei Links außerhalb der Tutor-Allowlist (`pruefeTutorLinks`, ADR-023) | Complexity metrics (verworfen: SonarQube abgelehnt (#28); nur Dateilänge ≤ 500 Zeilen); LLM code review (geplant: #24 KI-Code-Review vor jedem Merge); LLM design review (offen)
| Data & Schema | 2 von 2 (100 %) | Schema validation: Front Matter der Kompetenzen (`pruefeKompetenzen`) und Kartendaten (`src/karte/schemas/`); Config validation: App-Konfiguration: unbekanntes Fach bricht den Build (`lib/fachfarben.js`); Data contract: Karten-Knoten müssen existieren (`test/build/karte-daten.test.js`) | –
| UX & A11y | 3 von 3 (100 %) | Accessibility automated: axe-core, WCAG 2.2 AA, 0 schwere/kritische Verstöße (`e2e/axe.spec.js`), #26; Contrast checker: WCAG-Kontrast je Fachfarbe (`test/build/fachfarben.test.js`); UI prose lint: Tutor-Texte nennen keinen Wettbewerb (`pruefeTutorText` in `lib/pruefungen.js`), #29 | –
| Operations | 0 von 1 (0 %) | – | Runtime assertions (offen)
| Formal Methods | entfällt | – | –
| Documentation | 3 von 6 (50 %) | Link checker: `test/build/doku-verweise.test.js`, geprüfte serlo-Links, Tutor-Deep-Links; Diagram build: `doku.yml` baut PlantUML und prüft die Ausgabe in jedem PR; Prose lint: `tutor.md` unter 120 Zeilen (`MAX_TUTOR_ZEILEN` in `eleventy.config.js` bricht den Build), #29; Doc-code drift: Tutor-Vertrag: jeder Generator-Parameter steht in `llms.txt` | Markdown / AsciiDoc lint (offen); Code-in-docs validation (offen); Spell check (offen)
|===

Gesamt bis Tier 2: 18 von 29 zutreffenden Schichten (62 %); entfallene Schichten zählen nicht mit.
