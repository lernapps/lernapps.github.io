// Erzeugt von scripts/harness-rad.js – nicht von Hand ändern.
[cols="2,2,5,4",options="header"]
|===
| Abschnitt | Stufe (Tier 2) | Nachweis | Lücke
| Build & Language | 2 von 5 (40 %) | Compiler / Parser: `npm test` und `npm run build` laden jedes Modul; Syntaxfehler brechen `test-und-build`; Linter: ESLint (`eslint.config.js`: no-eval, no-implied-eval, no-unsanitized) im Pflicht-Check, #21 | Type checker (geplant: #25 `tsc --checkJs`, zuerst `src/kern`); Formatter (offen); Import sorter / dead code (offen)
| Testing | 2 von 3 (67 %) | Unit tests: rund 500 `node:test`-Tests in `test/` und `src/<app>/test/`; Property-based / fuzz: fast-check, 17 Properties, #22: Zahlen und Terme (`test/kern/*.property.test.js`, Fund #30 behoben); jede Rechnung im Lösungsweg jeder App wird nachgerechnet (`test/apps/rechenweg.property.test.js`, L-021); Integration tests: Vertragstest `test/apps/vertrag.test.js`: jede Kompetenz jeder App, Seeds 1–20; Contract tests: Tutor-Vertrag `lib/llms-vertrag.js`: Deep Links in `llms.txt`/`tutor.md` gegen Generator-Parameter | Smoke tests (offen)
| Security | 4 von 5 (80 %) | Secret scanning: GitHub Secret Scanning mit Push Protection; SCA: Dependabot-Alerts und -Security-Updates; `npm audit --audit-level=high` im Pflicht-Check, #20; SAST: CodeQL Default Setup (JavaScript/TypeScript, Actions); LLM security review: OWASP-Teil des KI-Reviews (`werkzeuge/review/ki-review.md`), Check `ki-review`, #24, ADR-027; Threat modeling: STRIDE in arc42 8.1 | License compliance (offen)
| Architecture | 2 von 4 (50 %) | Code review: Product Owner prüft und merged jeden PR; kein Pflicht-Approval; LLM code review: KI-Review in frischem Kontext vor jedem Merge (`werkzeuge/review/ki-review.md`), Check `ki-review`, #24, ADR-027; ADR enforcement: `lib/pruefungen.js` bricht den Build bei Regelverstößen (ADR-008), auch bei Links außerhalb der Tutor-Allowlist (`pruefeTutorLinks`, ADR-023) | Complexity metrics (verworfen: SonarQube abgelehnt (#28); nur Dateilänge ≤ 500 Zeilen); LLM design review (offen)
| Data & Schema | 2 von 2 (100 %) | Schema validation: Front Matter der Kompetenzen (`pruefeKompetenzen`) und Kartendaten (`src/karte/schemas/`); Config validation: App-Konfiguration: unbekanntes Fach bricht den Build (`lib/fachfarben.js`); Data contract: Karten-Knoten müssen existieren (`test/build/karte-daten.test.js`) | –
| UX & A11y | 2 von 3 (67 %) | Contrast checker: WCAG-Kontrast je Fachfarbe (`test/build/fachfarben.test.js`); UI prose lint: Tutor-Texte nennen keinen Wettbewerb (`pruefeTutorText` in `lib/pruefungen.js`), #29 | Accessibility automated (offen)
| Operations | 0 von 1 (0 %) | – | Runtime assertions (offen)
| Formal Methods | entfällt | – | –
| Documentation | 3 von 6 (50 %) | Link checker: `test/build/doku-verweise.test.js`, geprüfte serlo-Links, Tutor-Deep-Links; Diagram build: `doku.yml` baut PlantUML und prüft die Ausgabe in jedem PR; Prose lint: `tutor.md` unter 120 Zeilen (`MAX_TUTOR_ZEILEN` in `eleventy.config.js` bricht den Build), #29; Doc-code drift: Tutor-Vertrag: jeder Generator-Parameter steht in `llms.txt` | Markdown / AsciiDoc lint (offen); Code-in-docs validation (offen); Spell check (offen)
|===

Gesamt bis Tier 2: 17 von 29 zutreffenden Schichten (59 %); entfallene Schichten zählen nicht mit.
